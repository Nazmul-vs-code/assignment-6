import React from 'react';

const SelectedCards = ({setSelectedCarts , selectedCarts}) => {
    console.log(selectedCarts, "Selected carts are here")
    return (
        <div>
            hi, I am selected cards component
        </div>
    );
};

export default SelectedCards;